"use client";

export function DevelopersNav() {
  return (
    <div className="w-full bg-black text-white text-sm py-2 text-center">
      {/* Subnav ou aviso */}
      <span>"We build blocks  not just of code, but of trust." </span>
    </div>
  );
}
